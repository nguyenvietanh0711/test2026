apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-svc
  namespace: nginx-release-demo
spec:
  type: NodePort
  selector:
    app: nginx-demo
  ports:
    - name: http
      port: 80
      targetPort: 80